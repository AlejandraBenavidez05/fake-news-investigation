  spring:
    datasource:
      url: jdbc:sqlserver://localhost:1433;databaseName=konradquiz_dev;encrypt=false;trustServerCertificate=true
      username: sa
      password: KonradQuiz123!
      driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    jpa:
      hibernate:
        ddl-auto: update       # auto creates/updates tables locally — never in prod
      show-sql: true            # see queries in console while developing

  app:
    cors:
      allowed-origins: http://localhost:3000,http://localhost:5173  # React default ports
    encryption:
      secret-key: "dev-secret-32chars-change!!"    # exactly 32 chars for AES-256
    admin:
      password: "dev-admin-password"

  logging:
    level:
      com.konrad.konradquiz: DEBUG
      org.hibernate.SQL: DEBUG
      org.hibernate.orm.jdbc.bind: TRACE           # shows actual query parameter values